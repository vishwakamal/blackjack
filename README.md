# BlackJack Game

A blackjack simulator built with vanilla JavaScript, HTML, and CSS.

## How to Run
1. Clone this repository
2. Open a terminal in the project folder
3. Run: `python3 -m http.server 8000`
4. Open browser to: `http://localhost:8000`
